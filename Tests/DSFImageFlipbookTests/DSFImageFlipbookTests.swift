import XCTest
@testable import DSFImageFlipbook

final class DSFImageFlipbookTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(DSFImageFlipbook().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
